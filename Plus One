class Solution:
    def plusOne(self, digits):
        strings = ""
        for number in digits:
            strings += str(number) #"123"

        temp = str(int(strings) +1) # temp = str(int("123") + 1) = "124"

        return [int(temp[i]) for i in range(len(temp))]
